# Store Preview Live Check

The local production build is serving `http://localhost:4175/store` with HTTP 200.

Visible preview state:

- Store gate heading and return links render correctly.
- Moonberry Farm background preview renders with a readable dark overlay.
- Cyan Thread Glow and Gold Orbit Glow options render as keyboard-focusable selection buttons.
- Empty catalog state is clearly labeled `Preview only`.
- The Store route uses the living-world web background and AO semantic token styling.
- The external storefront link points to `https://anomartsy.lol/`; the current domain remains unresolved publicly and was not changed.

The browser viewport also shows the existing development accessibility overlay markers; these are tooling annotations, not application content.
